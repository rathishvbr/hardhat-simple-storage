{
  "name": "hardhat-simple-storage",
  "version": "1.0.0",
  "description": "First simple hardhat project",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.23.0"
  }
}
